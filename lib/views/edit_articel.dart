import 'package:flutter/material.dart';

class EditArticelScreen extends StatelessWidget {
  const EditArticelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('test'),
      ),
    );
  }
}
